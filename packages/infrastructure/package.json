{
  "name": "@commentia/infrastructure",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all",
    "diff": "cdk diff",
    "clean": "rm -rf cdk.out dist",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.161.0",
    "constructs": "^10.3.0",
    "@commentia/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "aws-cdk": "^2.161.0"
  }
}